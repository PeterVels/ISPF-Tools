/* REXX
   Is there a translate function that turns 1 into a, 2 into b,...,
   26 into z, 27 into aa, 28 into ab,..., 53 into ba etc.?
*/
signal on novalue
arg input
if input = '' then exit

output = ''
letters = 'abcdefghijklmnopqrstuvwxyz'
do while input > 0
 j = input // 26 /* e.g. 29 // 26 = 3 */
 if j = 0 then
  do
   j = 26
   input = input - 26
  end
 output = substr(letters,j,1) || output
 input = input % 26 /* e.g. 29 % 26 = 1 */
end

say output
return output
